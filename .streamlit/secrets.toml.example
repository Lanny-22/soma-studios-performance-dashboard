# Streamlit Community Cloud → App settings → Secrets
#
# TWO DIFFERENT PASSWORDS — do not mix them up:
#
# 1) DATABASE_URL  = Supabase *database* password (same string as local .env)
# 2) DASHBOARD_PASSWORD = password people type on the website login screen
#
# Copy DATABASE_URL from your working local .env file — character for character.

DATABASE_URL = "postgresql://postgres.nnrhqqlzitbiqdbdcolm:PASTE_DB_PASSWORD_HERE@aws-1-eu-north-1.pooler.supabase.com:6543/postgres"
DASHBOARD_PASSWORD = "password-for-sharing-the-link"

# Download alert email — DEFERRED (Supabase download_log only; leave Resend/SMTP unset)
# Option A: Resend (when re-enabled)
# Sign up at https://resend.com → API Keys → create key
# DOWNLOAD_ALERT_EMAIL = "info@somastudiosmt.net"
# RESEND_API_KEY = "re_xxxxxxxx"
# RESEND_FROM = "SOMA Analytics <onboarding@resend.dev>"

# Option B: SMTP (Gmail / Workspace App Password)
# SMTP_HOST = "smtp.gmail.com"
# SMTP_PORT = "587"
# SMTP_USER = "..."
# SMTP_PASSWORD = "..."
# SMTP_FROM = "SOMA Analytics <info@somastudiosmt.net>"
# SMTP_USE_TLS = "true"

# Logo is bundled as assets/SomaLogo.png — do not set LOGO_URL unless you override it.
